import React from 'react';
import { CustomDrawer } from './styles';

function Settings() {
    return (
        <CustomDrawer open={true} anchor={'right'}>
            sadasdas
        </CustomDrawer>
    );
}

export default Settings;
